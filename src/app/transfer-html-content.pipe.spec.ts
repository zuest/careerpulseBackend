import { TransferHtmlContentPipe } from './transfer-html-content.pipe';

describe('TransferHtmlContentPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferHtmlContentPipe();
    expect(pipe).toBeTruthy();
  });
});
